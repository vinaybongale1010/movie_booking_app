<%= form_for(@theatre) do |f| %>
  <% if @theatre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@theatre.errors.count, "error") %> prohibited this theatre from being saved:</h2>

      <ul>
      <% @theatre.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :seat_limit %><br>
    <%= f.number_field :seat_limit %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
